node_modules
npm-debug.log*

# Secrets and real config — never bake these into an image. Pass real values
# with `docker run -e` / `--env-file` / your orchestrator's secret store.
.env
.env.*

# Not needed to build or run the API.
.git
.gitignore
docs
tests
scripts
README.md
nodemon.json
eslint.config.js
*.log
.vscode
.idea

node_modules
npm-debug.log*

# Secrets and local config — never bake these into an image. Pass real values
# with `docker run -e` / `--env-file` / your orchestrator's secret store.
.env
.env.*
!.env.example

# Development-only data. Production runs the s3 storage driver; the local
# driver's files never belong in an image.
uploads

# Not needed to build or run the API.
.git
.gitignore
docs
tests
scripts
README.md
nodemon.json
eslint.config.js
*.log
.vscode
.idea

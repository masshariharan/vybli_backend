node_modules/
dist/
*.log
npm-debug.log*

# Secrets and real config — never commit it. There is one .env file and it
# holds real credentials; see the comment at the top of .env itself.
.env
.env.*

# Editor / OS cruft
.vscode/
.idea/
.DS_Store
Thumbs.db
